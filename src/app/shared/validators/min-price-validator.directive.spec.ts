import { MinPriceValidatorDirective } from './min-price-validator.directive';

describe('MinPriceValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MinPriceValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
