import { HttpUrlValidatorDirective } from './http-url-validator.directive';

describe('HttpUrlValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new HttpUrlValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
